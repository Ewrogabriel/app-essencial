image: node:18

stages:
  - install
  - build
  - deploy

cache:
  paths:
    - node_modules/

install:
  stage: install
  script:
    - npm install -g pnpm
    - pnpm install --no-frozen-lockfile
  artifacts:
    paths:
      - node_modules/

build:
  stage: build
  script:
    - npm install -g pnpm
    - pnpm run build
  artifacts:
    paths:
      - dist/

pages:
  stage: deploy
  script:
    - mkdir -p public
    - cp -r dist/* public/
  artifacts:
    paths:
      - public
  only:
    - main
